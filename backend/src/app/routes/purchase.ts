import { FastifyInstance } from 'fastify';

export default async function (fastify: FastifyInstance) {
  fastify.post('/purchase', async function () {
  // return { message: 'Hello API' };
  // Get fingerprint ID
  // Process payment
  // Unlock paywall for fingerprint ID
  });
}
